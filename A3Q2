#include <stdio.h>
#include <string.h>
#define N 1000

char s[N];
int top=-1;

void push(char c){ s[++top]=c; }
char pop(){ return s[top--]; }

int main(){
    char a[N];
    gets(a);
    int n=strlen(a);
    for(int i=0;i<n;i++) push(a[i]);
    for(int i=0;i<n;i++) a[i]=pop();
    puts(a);
}
